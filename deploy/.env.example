# The port exposed on the VPS. 80 is common when putting a reverse proxy in front.
APP_PORT=8080

# Absolute path on the VPS to the config.json you deploy manually.
CONFIG_PATH=/opt/k2marketing/config/config.json

# Directory with service-account and other secret files (can be empty).
SECRETS_DIR=/opt/k2marketing/secrets
GOOGLE_APPLICATION_CREDENTIALS=/secrets/vertex-ai.json

# Database credentials used by both postgres:16 and the Go app.
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=k2db
